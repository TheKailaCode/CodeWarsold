//Instructions

//Given an array of integers, return a new array with each value doubled.

function maps(x) {

    let newArr = x.map(x => x * 2)
    return newArr

}
  //use the map function to grab the element in the array
  //multiply the element times 2
  //map function returns a new array with updated element values doubled